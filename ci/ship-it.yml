---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-awscli

inputs:
  - name: dp-api-poc

params:
  AWS_DEFAULT_REGION:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  DEPLOYMENT_BUCKET:
  DEPLOYMENT_GROUP:

run:
  path: dp-api-poc/ci/scripts/ship-it.sh
